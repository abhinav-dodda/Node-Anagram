describe('Test the dictionary routes', () => {
    it('tests the GET /list url', () => {

    });

    it('tests the POST /add url', () => {

    });

    it('tests the POST /remove url', () => {

    });
})
